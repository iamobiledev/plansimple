{
  "name": "plansimple",
  "private": true,
  "version": "0.3.0",
  "description": "PlanSimple — cloud-native construction document collaboration",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @plansimple/e2e test",
    "db:migrate": "pnpm --filter @plansimple/api db:migrate",
    "db:generate": "pnpm --filter @plansimple/api db:generate",
    "seed": "pnpm --filter @plansimple/api seed"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.2"
  }
}
